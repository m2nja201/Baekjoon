#include <iostream>
using namespace std;

int main(){
    cout.tie(0);
    cin.tie(0);
    
    int n, k, a, b;
    cin >> n >> k;
    for (int i=0; i<n; i++){
        cin >> a >> b;
    }
    cout<< "비와이";
    return 0;
}